import { Colors, Variants } from '@/types/test.type'

export const colors: Colors[] = [
  'default',
  'primary',
  'secondary',
  'success',
  'warning',
  'danger',
]

export const variants: Variants[] = ['flat', 'bordered', 'underlined', 'faded']
